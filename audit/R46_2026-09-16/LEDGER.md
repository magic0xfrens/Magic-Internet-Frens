# R46 ledger — 2026-09-16

Branch `redteam/2026-09-13`, 23 commits on `a3bcd29`. `main` FROZEN at `0d2c39c`.
**Nothing pushed. Nothing on `main`.**

Rules: `tail -40` before your first edit. Post a `CLAIMED` row naming every file you
will touch. Never edit another agent's claimed file — post `WAIT` and report.
`git status` before committing. **Stage only claimed files — never `git add -A`.**
Flip to `DONE` with the commit hash. Commit green partials rather than lose work.

## P0 — orchestrator, verified before any agent ran

| claim (from brief) | result |
|---|---|
| 23 commits on `a3bcd29` | **VERIFIED** `git rev-list --count` = 23 |
| `main` frozen at `0d2c39c` | **VERIFIED** |
| `auto-deploy.sh:65` refuses chain ≠ 11155111 | **VERIFIED** exact line 65 |
| `auto-deploy.sh:81` EIP-170 guard reads `$4` not `$5` | **VERIFIED** — header is `Contract\|Runtime Size\|Initcode Size\|Runtime Margin\|Initcode Margin`, so with `-F'\|'` `$4`=Initcode Size (never negative) and `$5`=Runtime Margin. **The guard can never fire.** |
| B-3 is uncommitted, `V2B_GenesisOnlyQuorum.t.sol` untracked | **FALSE — brief is stale.** B-3 landed in `6eaf67c`: `_getVotingUnits` returns `genesisBalanceOf[account]` (`MiFrensGenesis.sol:721`). V2B is tracked (180 lines, committed). Working tree is clean. Only the **quorum-denominator trap** remains unverified. |
| (not in brief) | **NEW BLOCKER — `PerpEngine` runtime 25,185 B, margin −609 B. OVER EIP-170. Does not deploy.** Both 0.8.26 and 0.8.30 artifacts. `55ba6fe` (the E1A Critical fix) added 97 lines to the file. The `$4` bug at `auto-deploy.sh:81` is exactly why this was not caught. |

Also oversized, **test-only, not deploy blockers**: `Harness` in
`test/attacks/K3d_DeathBandProtectsForcedClose.t.sol` (−906), `X3iEngine` /
`X9cEngine` (−661, in `X3i_*`/`X9c_*` attack tests).

Contracts closest to the limit (runtime margin, B): CauldronRegistry **84**,
PoolOps **403**, PositionDescriptor **466**, CauldronHook **1,233**.

## Rows

| agent | finding | files (comma-separated) | functions | status | note |
|-------|---------|------------------------|-----------|--------|------|
| D1 | mainnet (4663) deploy path: no script existed | `scripts/deploy-mainnet-rh.sh` (NEW) | whole file | **DONE `8def6b1`** | `--self-test` proves 4 gates fire w/o RPC or key. D2 runs `--gates` then `--broadcast --stage deploy` against a fork. |
| D1 | EIP-170 gate reads `$4` (Initcode Size), can never fire | `scripts/auto-deploy.sh` | lines 64-84 | **DONE `8def6b1`** | now header-derived column + FAILS CLOSED. Line 65 chain assert kept, intent parameterised (`EXPECT_CHAIN`, default Sepolia). |
| D1 | selector gate RPC defaults to Sepolia; `vault` check could never run | `scripts/verify-selectors.mjs` | RPC default, chain assert, vault resolve | **DONE `8def6b1`** | RPC now derived from manifest `chainId` + asserted. `vault` resolved via `collection.vault()` (`CauldronCollection.sol:51`) so `redeem(uint256)` is checked for the first time. |
| D1 | 6 manifest keys with no producer | `indexer/deployments/round.robinhood.template.json` | `_producers` | **DONE `8def6b1`** | `chainId` is a 6th orphan the brief did not list — it stays 11155111 through a cutover unless hand-edited. 5 now produced; `indexerUrl` is genuinely underivable (Railway assigns it) and is a required input. |
| D1 | **CRITICAL (deploy config)** — `DEPLOY_QUOTES` defaults **true**, deploying `MockQuoteToken` whose `mint()` is **ungated** (`MockQuoteToken.sol:27`) and allowlisting it as a rotation destination | `scripts/deploy-mainnet-rh.sh` GATE 2 | — | **DONE `8def6b1`** (gated) | anyone could mint unlimited "Magic USD" and drain the rotation venue. Script refuses unless `DEPLOY_QUOTES=false`. **Root cause is in `DeployLaunchpad.s.sol:533` — not my claimed file; needs an owner decision.** |
| D1 | **HIGH (deploy config)** — `FEED_ETH_USD` (`DeployLaunchpad.s.sol:601`) is a **Sepolia** Chainlink address; VERIFIED `eth_getCode` = `0x` on 4663 → `usdPerRawUnit` returns 0 → hook records **zero volume for every trade**; mint ladder dead, `isDead` always true | `scripts/deploy-mainnet-rh.sh` GATE 2 | — | **DONE `8def6b1`** (gated) | the script's own comment (`:677-681`) documents this for "any other chain". `NATIVE_PEGGED_USD` is NOT the fix — 4663 native is real ETH. Reachable only via `DEPLOY_QUOTES=true`, already refused. |
| D1 | live r44 Sepolia deployment still missing `playChurn` | (none — observation) | — | **REPORTED** | the repaired selector gate caught it on the first run against the live manifest. Not a code change; the deployed round is still broken. |
| D1 | ledger bookkeeping | `audit/R46_2026-09-16/LEDGER.md` | Rows | **DONE** | |
| O1 | read-layer has no alarm: wire external monitor to `/freshness`, derive thresholds, price 4663 archive RPC, key rotation | `audit/R46_2026-09-16/LEDGER.md`, `indexer/scripts/patch-ponder-finality.mjs`, `indexer/src/api/index.ts`, `indexer/monitor/freshness-probe.mjs`, `indexer/monitor/README.md`, `docs/protocol/11-OPERATIONS.md` | `evaluateHealth`, `patchFinality`, `verifyFinality`, `ponderFinalityBlocks` | CLAIMED | staying in `indexer/` + `docs/`. NOT touching `contracts/`, `scripts/auto-deploy.sh`, `scripts/verify-selectors.mjs`, `indexer/deployments/round.json`. |
| S1 | EIP-170 PerpEngine over limit | contracts/solidity/cauldron/PerpEngine.sol, contracts/solidity/cauldron/PerpSwapLib.sol | size refactor only — no behaviour change | CLAIMED | PerpEngine runtime 25,185 B (−609). Freeing bytes. Nobody else edit these two files. |
| S1 | SPACE | contracts/solidity/cauldron/PerpEngine.sol | — | SPACE | need ≥609 B (target ≥987 more for the queued e1a split-band). VERIFIED regression point: 55ba6fe took PerpEngine 24,273 → 25,185 (+912). PerpEngine.sol is byte-identical from 0f71309 to 55ba6fe^, so 0f71309 = 24,273 B / +303 margin. |
| F1 | quorum denominator trap | contracts/solidity/test/attacks/V2C_QuorumDenominatorAtScale.t.sol (NEW, mine only) | n/a — read-only on cauldron/MiFrensGenesis.sol + cauldron/TreasuryGovernor.sol | DONE 17fe081 | NO FINDING — trap does not bite. Denominator is `getPastTotalSupply` (TreasuryGovernor.sol:996) = `Votes._totalCheckpoints`; `MiFrensGenesis._update:803` keeps badge units out of that trace, so numerator and denominator are the same trace. V2C: 1,111 sold out + 3,000 badges -> bar stays 111 (would have been 411), proposal executes. 2 passed. Side note: V2A_VoteFarm FAILS (not skips) without FORK_RPC — zero coverage in a no-fork CI. |
| O1 | (amend) monitor transport file added to claim | `.github/workflows/indexer-monitor.yml` | scheduled probe job | CLAIMED | new file; does not touch the existing `deploy.yml` |
| L1 | day-one launch caps + execute the stop paths that exist | `contracts/solidity/test/attacks/L1_LaunchStops.t.sol` (NEW, mine only), `audit/R46_2026-09-16/LEDGER.md` | n/a — read-only on `CauldronRegistry.sol`, `cauldron/CauldronBase.sol`, `cauldron/PerpEngine.sol`, `cauldron/PerpVault.sol`, `cauldron/MiFrensGenesis.sol` | **DONE `18033d1` + `b9d4ec4`** | 6 tests, **fork-free**, 6 passed / 0 skipped against the post-`c20671a` tree. No production file edited. |
| L1 | **the stop paths had never been executed** — `Z06` is the only test touching `setRedemptionPaused`/`vetoEmergency` and every test there is gated on `ZAuditBase.active`, set only when `FORK_RPC` is present (`ZAuditBase.sol:56-58`) | `test/attacks/L1_LaunchStops.t.sol` | — | **DONE `18033d1`** | now runs with no fork, so CI covers the breaker for the first time. |
| L1 | **L1-A (Medium, operational)** — arming an emergency silently DISENGAGES the redemption breaker while `redemptionPaused()` still returns true | (test only) | `_redeemBlocked` | **REPORTED** | `CauldronBase.sol:433-435` `redemptionPaused && emergencyReadyAt == 0`. The two emergency tools cannot be used together; re-pausing cannot re-close it. Custody rescue (`emergencyWithdrawLP`, `CauldronRegistry.sol:475`) is `timelocked`, so the 48 h rescue window is exactly when holders can race you to the exit. |
| L1 | **L1-C (Medium)** — `MAX_PER_WALLET`, the protocol's ONLY per-actor cap, is bypassable by transferring out and re-minting | (test only) | `MiFrensGenesis.mint` | **REPORTED** | cap tests `balanceOf(msg.sender)` (`MiFrensGenesis.sol:338`), a current holding not a lifetime allowance. PoC mints 40 under a cap of 20, one actor, no sybils. |
| L1 | brief's PerpEngine **setter** line numbers are all wrong (decl lines are right) | (none — observation) | — | **REPORTED** | post-`c20671a`: `setFees:2676`, `setRisk:2680`, `setGuards:2763`, `setVaultLimits:2803`, `setMinCollateral:2808`. Anchor by function name, not line. |
| R2 | blind re-hunt on the 4 un-audited fix commits (e0d4c9f gacha slots, f3d3f9e rotation derivation, 696899b stalled(), 6eaf67c presale cap) | `contracts/solidity/test/attacks/R2B_StalledDustImmunity.t.sol` (NEW), `contracts/solidity/test/attacks/R2D_RotationPrimaryDerivation.t.sol` (NEW), `contracts/solidity/test/attacks/R2E_GachaSeedPin.t.sol` (NEW), `audit/R46_2026-09-16/LEDGER.md` | n/a — read-only on `CauldronHook.sol`, `cauldron/GachaLib.sol`, `cauldron/TreasuryGovernor.sol`, `cauldron/RedemptionExt.sol`, `cauldron/MiFrensGenesis.sol` | CLAIMED | NEW test files only; no production file edited. Not touching `cauldron/PerpEngine.sol`/`PerpSwapLib.sol` (S1), `scripts/` (D1), `indexer/` (O1), `V2C_*` (F1), `L1_*` (L1), `YBase.sol`. **FORK_RPC IS AVAILABLE** — `https://ethereum-sepolia-rpc.publicnode.com` serves the Sepolia PoolManager `0xE03A…3543`; `YBase._boot` runs against it. |
| D2 | mainnet deploy REHEARSAL on a 4663 fork: execute D1's path end to end, prove the refusals, read MAX_PER_WALLET back from chain, answer FEED_ETH_USD for 4663 | `scripts/deploy-mainnet-rh.sh`, `scripts/verify-selectors.mjs`, `scripts/apply-deployment.mjs`, `contracts/solidity/deploy/DeployLaunchpad.s.sol`, `contracts/solidity/deploy/DeployPerp.s.sol`, `indexer/deployments/round.robinhood.template.json`, `audit/R46_2026-09-16/LEDGER.md` | gates 0-4, stages deploy/ignite/perp/manifest | CLAIMED | Fork rehearsal only — NEVER broadcasts to real 4663. Not touching `CauldronHook.sol` (F2), `cauldron/PerpEngine.sol`/`PerpSwapLib.sol` (S1), `cauldron/CauldronCollection.sol`/`cauldron/MiFrensGenesis.sol` (separate session), `indexer/src`/`indexer/monitor` (O1), any `test/attacks/*` (F1/L1/R2). I will NOT commit `indexer/deployments/round.json` — a fork rehearsal must not leave the live manifest pointing at fork addresses. |
| F2 | B-4 legacy floor buyback threshold is wei-written, compared against raw units of the live quote | `contracts/solidity/test/attacks/F2A_LegacyThresholdDecimals.t.sol` (NEW, mine only), `contracts/solidity/CauldronHook.sol` (`_maybeLegacyBuyback`, `legacyBuyStep`, `legacyThreshold` decl, `setLegacyBuyback` NatSpec) | `_maybeLegacyBuyback`, `legacyBuyStep` | **DONE `587a6ed`** (test only; CauldronHook.sol RELEASED, see note) | NOT touching `cauldron/PerpEngine.sol`/`PerpSwapLib.sol` (S1), `scripts/` (D1/D2), `indexer/` (O1), `cauldron/CauldronCollection.sol`/`cauldron/MiFrensGenesis.sol` (other session). CauldronHook is on the EIP-170 ceiling — SPACE row to follow if margin moves. |
| F2 | LIQ04 routability bound vs the T1a all-or-revert pre-sweep | `contracts/solidity/test/attacks/LIQ04_GasStarve.t.sol` | `test_LIQ04_PreSweepDoesNotStarveTheUsersSwap` | **DONE `b2b1ae9`** | investigating; will NOT weaken the assertion without quoting old vs new numbers and a written justification. |
| D2 | **RELEASE + WAIT** — `contracts/solidity/deploy/DeployLaunchpad.s.sol` is being edited by a CONCURRENT session (uncommitted: `DEPLOY_QUOTES` default flipped to false, `_preflightQuoteStack()`, `MockQuotesSepoliaOnly`, `_requireUsableFeed`). I claimed it but never edited it. **Dropping it from my claim; I will NOT stage or commit it.** | `contracts/solidity/deploy/DeployLaunchpad.s.sol` | — | **WAIT** | Their fix closes D1's CRITICAL at source and additionally makes the mock quote stack revert on any chain != Sepolia, so it is unreachable on 4663 by construction. Read-only for me from here. |
| D2 | **FAIL-OPEN in D1's repaired EIP-170 gate** — a build that dies AFTER printing the `Runtime Margin` header scans 0 rows, and 0 rows over the limit reads as "no deployable contract over EIP-170". `__NO_HEADER__` only catches a build that died BEFORE the header. Same defect class as the `$4`/`$5` bug it was written to fix. | `scripts/deploy-mainnet-rh.sh` | `eip170_over`, `run_eip170_gate` | **FIXED** | PROVEN: header-only log -> PASS; truncated to 10 of 803 rows -> PASS. Fixed to assert a minimum row count (`MIN_SIZE_ROWS`, 200) AND that every contract in `MUST_MEASURE` appears BY NAME. Re-tested: empty/junk/header-only/truncated all ABORT; both real logs pass at 801/803 rows. **NOTE: my first alarm was WRONG — the real crashed log DID hold a complete 801-row table (forge's linter panicked after printing it), so D1's gate was correct on that input. The fail-open is real but narrower than I first said.** |
| D2 | **FAIL-OPEN in `verify-selectors.mjs`** — the gate that exists to catch a missing `playChurn` printed `selector parity OK` and exited **0** on a manifest whose `contracts` was `{}`. All 11 checks SKIPped; `bad` stayed 0. | `scripts/verify-selectors.mjs` | key loop, exit | **FIXED** | PROVEN by execution. A SKIP is now an exemption, not a default: `ALWAYS_REQUIRED` keys missing => FAIL; `executed === 0` => refuse; `collection`/`vault` skips print an explicit "re-run after ignition or redeem() ships unverified" note. Live Sepolia manifest still behaves identically (and still reports `playChurn` ABSENT — **third sighting**, selector `0xdf70b5a4`, router `0x4658…2fda`). |
| D2 | **`FEED_ETH_USD` ANSWER for 4663** | (report) | — | **ANSWERED** | A real Chainlink **ETH/USD feed DOES exist on 4663: `0x78F3556b67E17Df817D51Ef5a990cDaF09E8d3A9`** — VERIFIED by raw eth_call: code 9,571 B, `decimals()`=8, `description()`="ETH / USD", answer `0x39bb8fec00` = **$2,479.60**. D1's Sepolia constant is codeless on 4663 (confirmed). **BUT its published heartbeat is 86,400 s (24 h) — all 57 feeds in Chainlink's robinhood-mainnet directory are 86400 — while the code ships `HB_ETH = 4 hours`. `QuoteOracle.sol:246` returns 0 whenever age > heartbeat, so a 4 h window against a 24 h feed reads "cannot judge" for up to 20 h of every 24: the SAME zero-volume outcome, so swapping in the right address alone does NOT fix it.** Measured age at rehearsal: 2.45 h — inside 4 h by ~5 min. **No sequencer-uptime feed exists on 4663** (0 hits across the directory); `QuoteOracle._sequencerOk` returns true when unset (`:352`), which is the only available behaviour — residual risk, not a bug. **Decimals are NOT mis-assumed**: `QuoteOracle.sol:248-258` reads the feed's own `decimals()` and normalises; the `18` at the `setFeed` call site is the TOKEN's decimals (`QuoteOracle.sol:128`), not the feed's. |
| D2 | **`docs/MAINNET_LAUNCH.md` is a launch trap — following it verbatim ships the CRITICAL** | `docs/MAINNET_LAUNCH.md` | :52, :53-56, :58-59 | **REPORTED (not fixed — not my file)** | (1) `:52` offers `PRESALE_MAXWALLET=100` as optional; required is **7**, and the code default is 100 (`DeployLaunchpad.s.sol:117`) — following the runbook lands the cap at 100. (2) `:53-56` is a bare `forge script --broadcast` with **no `DEPLOY_QUOTES=false`**, which under the pre-fix default deployed the ungated-mint `MockQuoteToken` as a rotation destination. (3) `:58-59` instructs `setTwapWindow(15)` post-deploy "as low as 1s", while `deploy-mainnet-rh.sh` enforces a **300 s** floor — and because the runbook applies it AFTER deploy via the timelock, the gate cannot see it. `PerpEngine.sol:2747` accepts anything >= MIN_TWAP (1 s), so nothing stops it. |
| F2 | B-4 VERIFIED as **HIGH**, not Low/coherence | `contracts/solidity/test/attacks/F2A_LegacyThresholdDecimals.t.sol` | 6-dec + 18-dec control | **DONE `587a6ed`** | Measured: $9,900,000 buffered on a 6-dec quote, `token bought 0`, buffer frozen. The 18-dec ERC20 control fires end to end ($99,000 in, $99,000 spent), so the ERC20 buyback path is LIVE and only the decimal count separated working from dead. The `:1125-1134` "ETH-layout only / native settle would revert" comment is **STALE** — `LegacyBuyLib.buyStep` settles sync->transfer->settle for ERC20 (`LegacyBuyLib.sol:263-275`) and `quoteIsCurrency0` comes from the registry allowlist (`CauldronHook.sol:660-663`). Regression: 2 passed, 0 failed, 0 skipped. |
| F2 | **COLLISION — unclaimed edit to `CauldronHook.sol`, which F2 held the only CLAIMED row on** | `contracts/solidity/CauldronHook.sol` | `_cacheLegacyThreshold`, `legacyThresholdRaw`, `setLiveKey`, `setLegacyBuyback` | **RELEASED — NOT STAGED BY F2** | A concurrent session landed its own B-4 fix in my claimed file with no ledger row. I did **not** revert, stage or commit it. It WORKS: my PoC flips green against it (buffer 9.9e12 -> 0, bought 9.9e12). I kept it rather than churn the file. **Two residuals for the owner**, both argued down from High: (1) it scales by **decimals, not value** — `0.02 ether` becomes `0.02 USDG` = **2 cents** on a 6-dec quote, ~3,500x under the "amortizes the nested-swap gas" purpose stated at `CauldronHook.sol:335`. This is precisely the unit-vs-value error `PerpSwapLib.sol:94-106` documents as red-team F-03 ("it turned '25 ether of pool depth' into $25"), in the opposite direction. Effect is a nested v4 swap inside nearly every user swap once any fee is buffered — gas regression, NOT loss (the self-call is gas-bounded and result-ignored, and `NoOutput`/`Slipped` roll the buffer back). The value-correct form is two `_toUsd` calls, which the hook already has at `:758`. (2) the cache refreshes only in `setLiveKey`/`setLegacyBuyback`; `setLiveKey` has exactly one caller, `CauldronRegistry._recordSeed` (`CauldronRegistry.sol:1802`), so it is refreshed on relaunch but NOT by the live rotation flip at `RedemptionExt.sol:631-670`, which re-points `generationQuote` and the perp engine and nothing else. (Pre-existing `_liveKey` staleness, not introduced by this fix.) |
| F2 | byte table — CauldronHook, before/after the concurrent B-4 fix | `contracts/solidity/CauldronHook.sol` | — | **MEASURED, no SPACE needed** | runtime **23,343 -> 23,762 B (+419)**; margin **1,233 -> 814 B**. Under 24,576. Both numbers from `forge build --sizes` under `FOUNDRY_PROFILE=cauldron`. Next fixer taking bytes from this contract: start from **814**, not 1,233. |
| F2 | `X ether` / `1e18` sweep (Target 1 step 3) | (none — read-only) | — | **REPORTED** | GUILTY: `CauldronHook.sol:336` (B-4, now fixed). CLEAN, verified by reading the comparison: `PerpEngine.sol:182` `minCollateral` + `:546` `tierDepthWei` both go through `_q()`/`PerpSwapLib.quoteFactor` (`:986`, `:1017`, `:880`) — the F-03 fix holds; `CauldronBase.sol:162` `AUTO_MIGRATE_FEE` is compared against `msg.value` (`CauldronRegistry.sol:1360`), native by construction; `CauldronSeeder.sol:142` `PRIME_MIN_WEI` is compared against a budget clamped to `address(this).balance` (`:405`), native by construction; `QuoteRotator.sol:574` `_usdLive(q, 1e18)` is a nonzero-ness probe and `usdPerRawUnit` is linear, so it is decimals-independent. **8-decimal oracle answers: CLEAN** — `QuoteOracle.sol:248-260` reads `feedDec` from the aggregator at call time and normalises `answer * 10**(18-feedDec)`, then `:277` divides by `10**quoteDecimals`; the Chainlink ETH/USD 8-dec feed on 4663 is handled. LOW/coherence, not fixed: `CauldronHook.sol:450` `oddsFullVolumeWei = 0.5 ether` vs a raw play size when **no oracle is wired** on a 6-dec generation — odds collapse toward 0. Both conversion sites (`CauldronGachaRouter.sol:129-144`, hook `_toUsd` at `:758`) are no-ops with the oracle unset, and both fail SAFE (players win less, never more). **Operational, not code: wire `quoteOracle` AND `CauldronGachaRouter.setOracle` in the same step as any non-18-decimal quote.** |
| F2 | tree does not compile as of this commit — **not mine** | `contracts/solidity/test/audit_full_scope/PreSweepLargeBookLocal.t.sol` (untracked) | — | **REPORTED** | `Error (9574): Type uint8 is not implicitly convertible to expected type int256` at `:63` — a destructuring of the position tuple with the wrong arity/order. Untracked and unclaimed, so I did not touch it; I measured around it with `--skip 'test/audit_full_scope/**'`. Whoever owns it: `forge test` is red tree-wide until it compiles. |
