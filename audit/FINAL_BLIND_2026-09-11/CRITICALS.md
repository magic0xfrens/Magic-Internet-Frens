# Criticals (append-only, one line per Critical the moment it is confirmed)

- ~~X6a~~ **DOWNGRADED Critical→Medium by verifier V6**: `QuoteRotator.sol:365-366` takes `max(minOut, oracleFloor)` with a 3% floor wired at deploy, so the loss is capped at a few percent, not ~100%. Escalates back to High if a rotation pair is unpriceable to `quoteOracle` (`_oracleFloor` fails open by design). Original claim: — src/components (TreasuryRotation.tsx:256): rotation minOut computed as `parseUnits(String(1 - maxSlip/100), destDecimals)`, a flat ~1 token never multiplied by the slice; a 25% slice of 100 ETH signs a 0.99 USDG floor against ~75,000 USDG; `rotateSliceFrom` is permissionless so the shortfall is MEV-extractable. PoC: /tmp/blind-h6-poc/minout.out
- X4a / X1a **CONFIRMED Critical by verifier V1** (upgraded from High; found independently by hunters 1 and 4) — CauldronHook.sol:1096 `fundLegacyBuffer()` is ungated+payable and books native wei into `legacyBuffer`; `legacyBuyStep` (:1065-1075) spends that number via LegacyBuyLib.buyStep settling in `key.currency0` (LegacyBuyLib.sol:87-94). On an ERC20-quoted generation: a 0.02 ETH donation or a RoyaltyRouter royalty pins the buffer above the hook's quote balance and permanently bricks the floor buyback; where quote is available it drains `relaunchAsset[q]` with no slippage bound and strands the ETH. Writers at :1317/:1340/:1096 disagree on denomination. PoCs: h1 test/attacks/X1a_LegacyBufferDenomination.t.sol, h4 test/attacks/X4a_LegacyBufferDenomination.t.sol
  V1 additions: `setAllowedQuote` (CauldronRegistry.sol:302) forces every ERC20 quote to currency0 so the `quoteIsCurrency0` gate (CauldronHook.sol:1027) is always true; the drained ERC20 is the protocol reserve `relaunchAsset[q]` and LegacyBuyLib moves the balance without debiting the counter, so `FeeRouteLib.send` then fails and the remainder locks; the donated ether is unrecoverable (only `legacyBuyStep` decrements the buffer); `releaseRelaunchAsset` is try/catch-swallowed at PoolOps.sol:1075 so relaunch launches underfunded rather than bricking. Critical rests on a 6-decimal stable quote (the case CauldronRegistry.sol:301 designs for); degrades to High on an 18-decimal quote.
- ~~X3a~~ **DOWNGRADED Critical→High by verifier V3**: mechanism confirmed exactly (`PerpEngine.sol:1098` `if (newQuote != quote && plv != 0)`), but the trigger needs a governance-approved rotation (`NoRotationApproved`, `allowedQuote`), so the chain is not permissionless end to end. Original claim: — PerpEngine.sol:1098 guards a quote rotation with `plv != 0` alone; `tokYieldEth`, `insuranceEth`, `payoutOwed` and PerpVault `pendingEth` survive the flip at full wei nominal and then claim that many units of the new asset; chain is permissionless (divergence → `_isDead` → `forceCloseAllDead` → `syncGeneration`). PoC: h3 test/attacks/X3a_QuoteRotationRedenominates.t.sol (plv claims 1000e18 vs 999e18 held; 1.5 ETH permanently unpayable)
- ~~X3c~~ **DOWNGRADED Critical→High by verifier V3** (sharpest of the four): `openCount == 0` forces `longOiEth == 0`, so `totalEth() = plv + longOiEth` means `plv == 0` is exactly when the queue is worst-backed, and that is when the guard passes; `_haircut`'s zeroing at PerpVault.sol:295 is always rolled back by the revert at :296, so the invariant documented at :89-90 is unenforceable. Original claim: — PerpVault: a stale exit queue survives the quote rotation; an 8 ETH queue takes 100% of a 1000 USDG depositor (control bounds it to 8e18). PoC: h3 test/attacks/X3c_StaleQueueSurvivesRotation.t.sol
- ~~X5a~~ **DOWNGRADED Critical→High by verifier V5**: the missing gate is real and the refund loss unrecoverable (only ETH exits are MiFrensGenesis.sol:305 and :584, no owner sweep), but `DeployLaunchpad.s.sol:524` defaults `finalizer` to the deployer, so the anyone-can-ignite form needs the opt-in `FINALIZER=0x0` config. Original claim: — MiFrensGenesis.sol:575-586 `igniteCauldron()` never checks `cancelled`; a cancelled sold-out presale passes every gate, so anyone (47k gas, 0 ETH) forwards the refund pot into `registry.summon{value: balance}()` and every un-refunded `paid[]` balance is permanently unrecoverable; `cancelPresale` (:289) has no sell-out precondition; front-runnable ahead of refunds. PoC: h5 test/attacks/X5a_GenesisCancelledIgnite.t.sol

## Found during the fix phase (not by the hunt)
- X4b-twin (fixE, VERIFIED, MEDIUM) — the same "discard the remainder" bug existed on the SELL side of the gacha churn: `CauldronGachaRouter.sol:504 tokBal = 0;` dropped unsold creature tokens from the end-of-churn sweep. Found by sweeping for the SHAPE of the reported bug rather than the instance. Fixed in 381b2a1 with its own regression test. Lesson for the report: every confirmed finding should get a shape-sweep across its neighbourhood.
- X4e (fixA, VERIFIED, **HIGH — value loss, worse than the finding that led to it**) — `FeeRouteLib._deliver`/`deliver` trusted the success flag of a low-level call to the perp engine. The ERC20 leg only mis-reported, but the NATIVE leg `to.call{value: amount}(nativeSel)` against a codeless address SUCCEEDED AND REALLY TRANSFERRED, so the stakers' 70% of every perp fee left permanently with no recovery path. Found by shape-sweeping the X4c codeless hole, not by any hunter. Fixed in a3773fc: extcodesize check before either branch, share rolls into relaunchETH/relaunchAsset[] which have a release path. Regression: test/attacks/X4e_CodelessDeliverTarget.t.sol (3 tests, -vv confirmed).

## Method notes for the final report
- TWO of the fix-phase discoveries came from sweeping for the SHAPE of a confirmed bug rather than its instance (gacha sell-side twin, FeeRouteLib._deliver twin). Neither was in any hunter's report. Recommend this as a standing step after every confirmed finding.
- EIP-170 technique that BACKFIRED: folding four duplicated call sites into one private helper COST 18 bytes in CauldronHook (24,533 -> 24,551) because via_ir re-inlines it. The brief lists this as the first technique to try; on this codebase it is wrong. What worked: extracting code into a new LINKED library (cauldron/SurtaxLib.sol), which freed ~333 B. Update the remediation playbook.
- X1e (orchestrator, VERIFIED by source reading, **HIGH — fix-induced, same day**) — the Critical fix `f9c775f` made `CauldronHook.fundLegacyBuffer()` revert `BadParam()` unless live currency0 is native (CauldronHook.sol:1136-1137). `cauldron/RoyaltyRouter.sol:33` is the EIP-2981 royalty receiver and its `receive()` forwards ALL value into that function unconditionally. On an ERC20-quoted generation every marketplace royalty payment now reverts; a marketplace pushing royalties atomically inside the sale reverts THE WHOLE SALE, breaking secondary trading. RoyaltyRouter has no owner, no sweep, no withdraw, so any ETH already held has no exit at any privilege level, and its comment at :20-21 claims the opposite ("the ETH stays here (recoverable by re-pointing), never lost"). Assigned to fixA.
  **Method note: this is the audit's own remediation reproducing the exact pattern the reconciliation documented (5 of this pass's top findings were fix-induced). It was found by tracing a DESIGN-INTENT question (owner-can-reclaim-via-timelock), not by any hunter or fixer. The neighbourhood re-hunt after P4 is therefore NOT optional.**
- X7a (docs pass, VERIFIED by source reading, **HIGH — user funds, every trade**) — `SwapWidget.tsx:96` and `:103` sign `minOut = 0n` on every spot buy AND sell. The router and hook both accept a floor; only the widget never supplies one. Every ordinary user trade is fully sandwichable. The perp (`6a275c5`) and rotation (`50839dc`) minOut fixes did NOT cover the path users actually use most. Assigned to fixF.
- X7b (docs pass, VERIFIED, MEDIUM) — `scripts/marketmaker.sh:63` calls a 4-arg `play(...)` against a 5-arg router (`CauldronGachaRouter.sol:233`); every buy reverts and `:64` swallows it as an "RPC hiccup", so the market maker has been silently dead. Third selector-mismatch of the run (LaunchSniper, perp UI, this).
- X7c (docs pass, VERIFIED, LOW) — fixD's deployment-record fix `a6a56f8` points the SUPERSEDED markers at `deployments/sepolia.json`, which self-declares CANONICAL at `:2` but holds ROUND 20 data while the live system reads round 38 from `indexer/deployments/round.json`. Fix-induced mis-pointer. Assigned back to fixD.

**Method note: the DOCUMENTATION pass found a High that six hunters, six verifiers and six fixers all missed — because writing down "where does minOut come from for every write path" forced an exhaustive enumeration that attack-driven review never performed. Documenting a system as a table of every path is itself an audit technique.**

## Coherence findings from the documentation pass (protocol claims vs code)
- The published holder tax TIERS do not exist. `CauldronHook.sol:111-113` lists Wizard/King/Knight/Peasant rates; no contract in the tree implements `getHolderTaxRate` (interface-only). Everyone pays `defaultTaxBps` = 300 bps. The public doc `src/components/docs/magicfrens-llm.md:349` claims a holder "still gets their own tier".
- The ETH floor vault is NEVER funded. Both deployment paths call `hook.setVault(0)` (`CauldronRegistry.sol:1171`, `:1195`), so `CauldronVault.redeem` always reverts `UnifiedFloorActive()`. The live floor is `CollectionLedger`, denominated in the iteration token, not ETH.
- The public doc says the surtax is the MAXIMUM of decay and jitter (`magicfrens-llm.md:318`); the code adds them (`d3c10a1` changed `max(decayed, jitter)` to `decayed + jitter`). User-facing and stale.
- The hook header understates its own permission bits (claims 3, enables 5).
- LEAD for the re-hunt, not yet a finding: the anti-sniper window is counted in BLOCKS while the volume window is in SECONDS — the exact asymmetry the volume clock was changed to avoid.
- 14 further comment-vs-code disagreements recorded in docs/protocol/06-HOOK.md, 07-FEES.md, 08-NFT-ECONOMY.md.

**Method note: the documentation pass produced coherence findings no hunter did, because writing "what does this actually do" forces a claim-by-claim reconciliation that attack-driven review skips.**
- `hasClaimed()` ALWAYS RETURNS FALSE (LOW, VERIFIED) — `CauldronBase.sol:209` declares `mapping(uint256 => mapping(address => bool)) public claimed;` and `CauldronRegistry.sol:1774-1775` exposes it; grep confirms NO WRITER anywhere in the non-test tree. Explicitly NOT a double-claim bug: nothing gates on it, so no claim path is weakened. It is a public view that lies to integrators, on a protocol whose core promise is per-generation claim guarantees. Assigned to fixC with instructions not to shift the shared facet storage layout. Severity kept at Low deliberately — the alarming reading (double claims) was checked and refuted.
- Registry has NO `fallback()` (documented, not a new finding) — every facet function needs an explicit stub or the call reverts as an unknown selector, and `CauldronRegistry.sol:1778-1782` says the OPPOSITE. Five functions have already shipped broken this way across the project's history. This is the root cause behind X2b and X2g.
