# R45 Byte Ledger — CauldronHook / PerpEngine / EIP-170-adjacent contracts

| contract | runtime bytes at P0 | free at P0 | free now | claimed by |
|---|---|---|---|---|
| CauldronHook | 23,140 | 1,436 | 1,436 | — |
| Harness (test/attacks/K3d_DeathBandProtectsForcedClose.t.sol) | 24,548 | 28 | 28 | — |
| CauldronRegistry | 24,492 | 84 | 84 | — |
| X3iEngine | 24,299 | 277 | 277 | — |
| X9cEngine | 24,299 | 277 | 277 | — |
| PerpEngine (0.8.26 / 0.8.30) | 24,247 | 329 | 329 | — |
| PositionDescriptor | 24,110 | 466 | 466 | — |
| PoolOps | 24,006 | 570 | 570 | — |
| PerpSwapLib (0.8.26 / 0.8.30) | 8,978 / 8,968 | 15,598 / 15,608 | 15,598 / 15,608 | — |
| GachaLib | 1,466 | 23,110 | 23,110 | — |

Note: rows above are every production/test contract in the real-tree `forge build --sizes` table with < 2,000 B free at P0, plus PerpSwapLib and GachaLib (required by brief regardless of margin). Harness/X3iEngine/X9cEngine/PositionDescriptor/PoolOps are test-harness or vendored-library contracts, not deployed production contracts — included here only because they crossed the <2,000 B threshold; treat CauldronHook, CauldronRegistry, PerpEngine as the load-bearing production rows.

## Claims table

| agent | finding | files (comma-separated) | functions | status | note |
|---|---|---|---|---|---|

Status legend:
- CLAIMED → DONE
- CLAIMED → RELEASED
- WAIT (note names the blocker)
- SPACE (note says bytes needed and from where)

## fixer-off claims (off-chain group)

| agent | finding | files (comma-separated) | functions | status | note |
|---|---|---|---|---|---|
| fixer-off | F6 build hygiene | contracts/solidity/foundry.toml | [profile.cauldron] skip | DONE 763a84b | clean `forge build --force` now succeeds from an empty out/; ALL runtime sizes identical to sizes-real.txt (CauldronHook 23,140 / CauldronRegistry 24,492 / PerpEngine 24,247 / GachaRouter 8,814). No ABI change. |
| fixer-off | F1 R5A | src/components/cauldron/CrystalCauldronGame.tsx, src/components/cauldron/TheCauldron.tsx, audit/R45_READINESS_2026-09-15/hunt/h5-poc/r5a-spin-quote-unaware.mjs | spin, Props | DONE cf188a6 | the crystal spin now sends quoteIn + a bounded approval when the quote is ERC20 (value only when native) and sizes its floor in quote units; CrystalCauldronGame takes quote/quoteSymbol/quoteDecimals. |
| fixer-off | F2 R5B | src/components/cauldron/StakePanel.tsx, src/hooks/usePerpVault.ts, audit/R45_READINESS_2026-09-15/hunt/h5-poc/r5b-stakepanel-decimals.mjs | onAction, approveQuote, approveToken | DONE d53c26c | StakePanel parses with the live quote decimals and ERRORS on over-balance instead of substituting the balance; usePerpVault.approveQuote/approveToken now TAKE an amount (signature change for callers) and approve exactly it. |
| fixer-off | F3 R5C | indexer/ponder.config.ts | chains.cauldron.rpc | DONE 52e9c3c | default is ONE pinned provider (tenderly); PONDER_RPC_URL as a list is viem fallback(rank:false) ordered failover, never round-robin. railway.json untouched. |
| fixer-off | F4/F7 R5E,R5F | indexer/src/api/index.ts | evaluateHealth, watchdog, /candles, /recent | DONE a2c2e0b | /freshness now separates BEHIND (ok) from DIVERGED (not ok) and no longer masks divergence with everHealthy; watchdog fires on a fresh boot. Response shape kept + new `behind` field. /candles/:gen and /recent/:gen 400 on a non-integer param. |
| fixer-off | F5 C-1 | scripts/verify-selectors.mjs, scripts/apply-deployment.mjs, scripts/auto-deploy.sh, scripts/deploy-testnet.sh, scripts/go-testnet.sh, .github/workflows/deploy.yml | REQUIRED map, gate | DONE d9975af | selector parity now runs (and ABORTS) from apply-deployment.mjs, auto-deploy.sh and CI; REQUIRED covers 11 manifest keys / 43 signatures derived from the app's ABIs. Live run: ONLY playChurn missing on r44's router (0xdf70b5a4) — everything else present. deploy-round.mjs untouched (writes no manifest). Deploy scripts force a clean build before broadcast. |
| fixer-off | F7 R5H | src/hooks/useCauldronSwap.ts, src/components/cauldron/SwapWidget.tsx | approveToken | DONE 85a7a80 | useCauldronSwap.approveToken now TAKES an amount (signature change) and approves the sale size, not maxUint256. |
| fixer-off | F7 R5G | scripts/keeper.sh | materialize, sweep | DONE 7863119 | cast send failures log to stderr with the id; O(nextId) sweep logged-not-fixed in a comment. |

## fixer-vault claims (PerpVault group)

| agent | finding | files (comma-separated) | functions | status | note |
|---|---|---|---|---|---|
| fixer-vault | R2B | contracts/solidity/cauldron/PerpVault.sol, contracts/solidity/test/attacks/R2B_TokenQueueLatch.t.sol, contracts/solidity/test/attacks/R2Mock.sol | claimPendingToken, depositToken, _bankTokWriteDown | DONE 8bcfe78 | claimPendingToken now BANKS the haircut on both owed==0 and paid==0 (no more revert-rollback), so pendingTok drains and hasStakers()/setVault unlatch; depositToken now reverts QueueInsolvent while pendingTok > engine.totalTokenAssets(). ABI CHANGED (see R2C row). |
| fixer-vault | R2C | contracts/solidity/cauldron/PerpVault.sol, contracts/solidity/test/attacks/R2C_DepositLatch.t.sol | claimPendingEth, _bankEthWriteDown, settlePendingEth, settlePendingToken | DONE 8bcfe78 | ANYONE can now call settlePendingEth(user)/settlePendingToken(user) to bank a queued address's pro-rata write-down; it pays the caller nothing, so a hostile receive() cannot re-latch. QueueInsolvent gate itself UNCHANGED and still refuses deposits while the queue is unbacked. ABI CHANGED: PerpVault gains settlePendingEth(address), settlePendingToken(address), event QueueWrittenDown(address indexed,bool,uint256) — indexer/abis + src/config may want the new event. |
| fixer-vault | R2A | (none) | _haircut | RELEASED | ACCEPTED-BY-DESIGN, no source change. A queued exit is a fixed nominal that has LEFT the share base (PerpVault.sol:336-344), so it stops earning yield as well as bearing loss; making it keep bearing loss after its shares are burned needs a second share class with its own index — i.e. the restructure of the queue's economics the brief forbids, and it would re-open the yield-attribution ambiguity the nominal-claim model settles. The transfer is LP-vs-LP, bounded by _haircut once claims exceed backing (PerpVault.sol:366-374), and no value leaves the protocol. A future change must FIRST prove (a) the race is orderable in practice — an LP observing an incoming death-settle/liquidation and landing withdrawEth ahead of it on the target chain's ordering, not a PoC that hard-codes the sequence — and (b) that the expected transfer exceeds the yield forgone by queueing early. Neither is established today. |
| fixer-vault | pre-existing/other-agent | contracts/solidity/test/attacks/S06_PerpVaultSolvency.t.sol | test_S06_POC_RealEngine_QueueingBeforeBadDebtShedsAllOfItOnLpB | WAIT | NOT MINE — FAILS at :600 `pending 344218925886143795 <= backing 459182015833333191`. ETH-only real-engine run; my diff changes no ETH-side arithmetic (claimPendingEth refactor is behaviour-identical, settlePending* is never called there, the depositToken guard is unreachable on this path). Blocker: fixer-liq's in-tree PerpEngine.sol/CauldronHook.sol edits change how much bad debt is realised, which is exactly the number this asserts. fixer-liq to re-check. |

## fixer-gacha claims

| agent | finding | files (comma-separated) | functions | status | note |
|---|---|---|---|---|---|
| fixer-gacha | R3A uncapped gacha re-anchor | contracts/solidity/cauldron/GachaLib.sol, contracts/solidity/test/attacks/R3A_GachaReRollGrind.t.sol, contracts/solidity/test/attacks/R3C_SupplyConservation.t.sol, contracts/solidity/test/attacks/R3D_AgedQueueStall.t.sol, contracts/solidity/test/attacks/K4b_GachaReanchorGrind.t.sol | resolveTickets | DONE (d21cfbe) | DONE d21cfbe: a gacha batch now gets exactly ONE expiry re-anchor (ERC-7201 hashed slot in hook storage); a second expiry commits a LOSS instead of re-rolling. No CauldronHook.sol edit, Batch struct + batches(uint256) getter ABI UNCHANGED. GachaLib 1,466 -> 1,652 B (22,924 free). K4b inverted (it encoded the bug). R3C supply conservation still passes. |

## fixer-liq claims (in-swap liquidation group)

| agent | finding | files (comma-separated) | functions | status | note |
|---|---|---|---|---|---|
| fixer-liq | R1C caller-picked gas disables liquidation | contracts/solidity/CauldronHook.sol, contracts/solidity/test/attacks/R1C_GasFloorBypass.t.sol | _liqSweep, IPerpEngineLiq | DONE b8b7ab9 | DESIGN CHOICE: fail loudly. The PRE-trade sweep call now reverts `LiqGasStarved()` when gas is under the floor AND `perpEngine.openCount() != 0`; empty book = no gas floor, so routability is untouched. Post-trade sweep still degrades silently by design. LIQ_GAS_MIN/LIQ_GAS_RESERVE unchanged (no L-2 burn). TRADE-OFF: a swap on a pool with a live perp book supplying under ~1.05M gas now reverts. NEW ABI: `error LiqGasStarved()` on CauldronHook + `openCount()` added to the hook's local IPerpEngineLiq (PerpEngine already had the public var; PerpEngine ABI unchanged). Frontend/indexer: decode this error on failed swaps. CauldronHook 23,140 -> 23,258 B (1,318 free). |
| fixer-liq | R1B 12-slot window over 64-slot book | contracts/solidity/cauldron/PerpEngine.sol, contracts/solidity/test/attacks/R1B_SweepWindowStarvation.t.sol | _doSweep | DONE bfff978 | `SWEEP_SCAN = 12` REMOVED; `_doSweep` now makes one pass over the whole <=64-slot book, bounded by the pre-existing `gasleft() < SWEEP_KILL_RESERVE` break. A dust-padded book can no longer hide a victim from its own trade's sweep. Sweep gas is now book-length-dependent but hard-capped by the reserve break, so it still cannot OOG the parent swap. Measured worst-case padded sweep 30.79M gas in-test (40 pads + victim). PerpEngine ABI unchanged; 24,247 -> 24,215 B (361 free, 32 B SMALLER). |
| fixer-liq | R1A free-kill slack (LOG ONLY) | contracts/solidity/test/attacks/R1A_FreeKillSlack.t.sol | — | DONE bfff978 | ACCEPTED-WITH-RATIONALE, no source change. PoC kept as a characterization test that PINS the early-kill overshoot inside the SLACK_BPS band (measured 31.81 mETH on a 0.931 ETH principal) instead of forbidding it. SLACK_BPS unchanged at 1500; maintenanceBps still NOT applied to the projected leg (that was tried 2026-09-13 and reverted). Do not 'improve' this. |

## fixer-pool claims (seed/relaunch group)

| agent | finding | files (comma-separated) | functions | status | note |
|---|---|---|---|---|---|
| fixer-pool | R4A seed reserve stranded by a short-returning branch | contracts/solidity/cauldron/PoolOps.sol, contracts/solidity/test/attacks/R4A_SeedFundingStrand.t.sol | seedFunding, _pullAsset, _pullEth | DONE c6aefd0 | seedFunding now PEEKS hook.relaunchETH()/relaunchAsset() and only pulls when the branch would clear MIN_SEED_UNITS, so a declining branch leaves the reserve on the hook instead of stranding it loose in the registry; funded path unchanged; no ABI change; MIN_SEED_UNITS left as-is (673,940,070 base units, decimal-blind) - with the peek in place a sub-floor reserve costs nothing but a fall-through, so scaling it by quote decimals would need a try/catch decimals() call in the tightest-byte library for no safety gain; PoolOps 24,205 -> 24,173 B (403 free), CauldronRegistry untouched at 24,492 (84 free). OLD NOTE: peek hook reserve before pulling; CauldronHook.sol READ-ONLY (owned by fixer-liq), no edit planned |
| fixer-pool | R4A stub fidelity | contracts/solidity/test/audit/B13_SeedFundingDustPreference.t.sol | HookStub13 | DONE c6aefd0 | rename stub slots to relaunchAsset/relaunchETH to match the real hook's public getters; no assertion weakened |
| orchestrator | S06 WAIT resolution | contracts/solidity/test/attacks/S06_PerpVaultSolvency.t.sol | test_S06_POC_RealEngine_QueueingBeforeBadDebtShedsAllOfItOnLpB | DONE (no change) | fixer-vault routed a WAIT to fixer-liq suspecting the pre-emptive-liquidation changes moved this number. FALSE ALARM: the failure is byte-identical to the P0 baseline recorded before any fix in this run — "344218925886143795 <= 459182015833333191" (BASELINE.md:46). Pre-existing, not a regression, nobody owns it. fixer-liq told to ignore. NOTE: this test uses `vm.skip(!active)` in its top-level test_ function, so it reports SKIPPED rather than failing when the fork env is absent — logged as a coverage-quality item, not changed. |
| fixer-off | A LiqGasStarved (b8b7ab9 follow-up) | src/config/cauldron.ts, src/config/perp.ts | HOOK_ABI errors, PERP_ERROR_HELP/SELECTORS | DONE 09ae2dd | ABI-visible: HOOK_ABI now declares `error LiqGasStarved()`; 0x38dc5cd1 maps to an actionable 'raise the gas limit' message via explainPerpError. PerpPanel already routes through explainPerpError. RESIDUAL: SwapWidget.tsx:308-309 and CrystalCauldronGame.tsx:221-222 print viem's raw shortMessage, so they show the decoded NAME 'LiqGasStarved()' but not the actionable text — 2-line change, NOT made (staging was restricted to the two config files). SwapWidget.tsx/CrystalCauldronGame.tsx RELEASED, unclaimed. |
| fixer-off | B PerpVault settle* (8bcfe78 follow-up) | src/config/perp.ts | PERP_VAULT_ABI, PERP_ERROR_HELP | DONE 09ae2dd | ABI-visible: PERP_VAULT_ABI gains settlePendingEth(address)/settlePendingToken(address) + event QueueWrittenDown(address,bool,uint256). NOT added to verify-selectors REQUIRED (no app code calls them yet — correct per brief). Coherence gap recorded: no UI action calls them; the QueueInsolvent text names them, an operator/keeper must run them. |
| orchestrator | R1C follow-up (revert surface) | src/components/cauldron/SwapWidget.tsx, src/components/cauldron/CrystalCauldronGame.tsx | catch handler, friendlyErr | DONE e614101 | fixer-off honestly flagged that both files printed viem's raw shortMessage, so the new LiqGasStarved() would reach the user as a bare selector and the actionable "raise the gas limit" text would never show. Both now route through explainPerpError (which also decodes by selector and handles wallet rejection); curated help text is no longer truncated at 140 chars, only unrecognised raw strings are. tsc clean, npm run build clean (5 prerendered routes). |
| orchestrator | RESIDUAL (not fixed, recorded) | — | PerpVault.settlePendingEth/Token | OPEN | A user blocked by QueueInsolvent still has no in-app path to clear it. The ABI and help text name settlePendingEth/settlePendingToken, but nothing in src/ calls them and the indexer does not index PerpVault at all, so QueueWrittenDown has no consumer and the app has no feed of queued holders to supply the required target address. fixer-off's judgement, which I accept: a button with no way to choose its argument would be worse than the text; this belongs in a keeper/operator script that enumerates pendingEthOf/pendingTokOf from logs. Carried to FUNCTIONAL_COHERENCE.md and the final report. |
| fixer-pool | R4A stub fidelity (2 more) | contracts/solidity/test/attacks/Z1_SeedPriceDenominationFloor.t.sol, contracts/solidity/test/attacks/X5c_VaultSweptDenomination.t.sol | ReserveStub, X5HookMock | DONE c6aefd0 | add relaunchETH()/relaunchAsset() view getters the real hook already has; no assertion changed |

| fixer-vault | NB settle spam (fix-induced CRITICAL from 8bcfe78) | contracts/solidity/cauldron/PerpVault.sol, contracts/solidity/test/attacks/NB_SettleSpamHaircut.t.sol | pendingEth, pendingEthOf, pendingTok, pendingTokOf, _syncEthQueue, _syncTokQueue, _queueEth, _queueTok, _dropEthUnits, _dropTokUnits, settlePendingEth, settlePendingToken, claimPendingEth, claimPendingToken, withdrawEth, withdrawToken, deposit, depositToken, hasStakers, hasQuoteStake, assetsEth, assetsTok, ethPosition, tokenPosition | DONE beedf49 | The exit queue is now UNITS x a single INDEX. settlePendingEth/Token no longer touch any per-user state: a shortfall is recognised ONCE globally by scaling ethQueueIndex/tokQueueIndex, so the write-down is idempotent (50 calls == 1) and order-independent (A-then-B == B-then-A; equal claims pay 5.000/5.000). Repeated settling can no longer move a queued LP's claim to the caller. R2B/R2C unchanged in behaviour: QueueInsolvent still refuses deposits while a queue is unbacked, a total wipe-out still retires the queue (epoch bump) so hasStakers releases, settlePending* still transfers nothing. ABI: pendingEth()/pendingEthOf(address)/pendingTok()/pendingTokOf(address) KEEP their selectors and return types (public vars -> views). NEW getters ethQueueUnits/ethQueueIndex/ethQueueEpoch + token twins. QueueWrittenDown CHANGED to (bool indexed tokenSide, uint256 writtenOff, uint256 newIndex) - never shipped, introduced in 8bcfe78. indexer/abis + src/config must re-sync. |
| fixer-vault | S06 pre-existing failure (re-confirmed) | contracts/solidity/test/attacks/S06_PerpVaultSolvency.t.sol | test_S06_POC_RealEngine_QueueingBeforeBadDebtShedsAllOfItOnLpB | WAIT | STILL NOT MINE. Fails with BIT-IDENTICAL numbers before AND after the beedf49 restructure (pending 344218925886143795 <= backing 459182015833333191), which rules out both of my commits as the cause. Blocker: fixer-liq's in-tree PerpEngine.sol/CauldronHook.sol edits change how much bad debt is realised. fixer-liq to re-check. || fixer-liq | R1C follow-on: S08 PoCs encoded the pre-fix behaviour | contracts/solidity/test/attacks/S08_InSwapGasStarvation.t.sol | test_S08_B_PoC_SwapperChosenGasSilentlySkipsTheSweep, test_S08_D_MEASURE_DoomedSweepBurnsTheSwappersGas, header | CLAIMED | invert onto post-b8b7ab9 behaviour; no other agent file touched |
