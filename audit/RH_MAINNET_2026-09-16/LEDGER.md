# Ledger — RH mainnet review 2026-09-16

## Baseline sizes (forge build --sizes, contracts/solidity @ redteam/2026-09-13 a3bcd29)
| Contract | Runtime (B) | Margin (B) | Claimed | Match |
|---|---|---|---|---|
| CauldronHook | 23,258 | 1,318 | 23,258 (1,318 free) | yes |
| CauldronRegistry | 24,492 | 84 | 24,492 (84 free) | yes |
| PerpEngine | 24,215 | 361 | 24,215 (361 free) | yes |
| PoolOps | 24,173 | 403 | 24,173 (403 free) | yes |
| PerpVault | 11,280 | 13,296 | 11,280 | yes |
| GachaLib | 1,652 | 22,924 | 1,652 | yes |
| PerpSwapLib | 8,968 | 15,608 | 8,968 | yes |

No contract at or over the EIP-170 24,576 limit.

| agent | finding | files (comma-separated) | functions | status | note |
|-------|---------|------------------------|-----------|--------|------|
| chain-config-fixer | T5A,T5B,T5D,T5E,0a,0b,0c | src/config/deployments.ts,src/config/chains.ts,src/config/cauldron.ts,src/config/quotes.ts,src/hooks/useAllowedQuotes.ts,src/components/cauldron/TheCauldron.tsx,src/components/cauldron/SwapWidget.tsx,indexer/ponder.config.ts,indexer/src/api/index.ts,indexer/deployments/round.robinhood.template.json,scripts/verify-manifest.mjs,.env.example,docs/MAINNET_LAUNCH.md,docs/protocol/09-FRONTEND.md,docs/protocol/11-OPERATIONS.md,audit/RH_MAINNET_2026-09-16/poc/h5/* | DEPLOYMENTS/resolveInitialChain, IS_TARGET/ACTIVE_CHAIN, manifest-chain assert, quoteMeta, ponder rpc defaults, API_RPC_URL defaults | DONE | superseded by the 4 rows below; also touched indexer/deployments/active.ts (added to claim). No Solidity, no ABI change. |
| chain-config-fixer | T5A (Critical) | src/config/deployments.ts,src/config/chains.ts,src/config/cauldron.ts,scripts/verify-manifest.mjs | DEPLOYMENTS, resolveInitialChain, CHAIN_DEFAULTS, NATIVE_DECIMALS | DONE 800271a | DEPLOYMENTS is keyed by each manifest's OWN chainId, so a 4663 round.json targets 4663. VITE_CHAIN_ID is authoritative: a chain no manifest declares THROWS at module init and FAILS prebuild. A manifest/chain mismatch now THROWS in cauldron.ts (was console.error) — anything relying on the app still rendering on a mismatch will now blank instead. Per-chain name/RPC/explorer/currency defaults replace the flat Arc ones; unknown chain + no VITE_RPC_URL throws. NATIVE_SYMBOL/NATIVE_DECIMALS read ACTIVE_CHAIN, so VITE_CHAIN_DECIMALS is no longer discarded. No ABI change. |
| chain-config-fixer | T5B (High) | indexer/ponder.config.ts,indexer/src/api/index.ts,indexer/deployments/active.ts | rpc defaults, pollingInterval, API_RPCS, chain guard | DONE f59425e | Sync AND API RPC defaults are keyed by the manifest's chainId (4663 -> rpc.mainnet.chain.robinhood.com, 46630 -> testnet host); an unmapped chainId with PONDER_RPC_URL/API_RPC_URL unset now THROWS at startup instead of silently using Sepolia. active.ts's chain guard covers every DEPLOYMENT slot and honours a CHAIN_ID env declaration. Operators must set PONDER_RPC_URL or add the chain. No ABI change. |
| chain-config-fixer | T5D (Medium) | src/config/quotes.ts,src/hooks/useAllowedQuotes.ts,src/components/cauldron/TheCauldron.tsx,src/components/cauldron/SwapWidget.tsx | quoteMeta, useQuoteDecimals, SwapWidget priceable | DONE 72cff8b | Quote decimals are read on-chain (new useQuoteDecimals -> ERC20 decimals(), validated 0..36, null when unknown) instead of from the manifest. SwapWidget gains quoteDecimalsKnown and is UNPRICEABLE / refuses to sign while decimals are unresolved — a trade panel can now show "cannot price" for a second after a quote flip. quotes.ts reads ACTIVE_ROUND, not round.json. No ABI change. |
| chain-config-fixer | T5E + docs 0a/0b/0c | .env.example,indexer/deployments/round.robinhood.template.json,docs/MAINNET_LAUNCH.md,docs/protocol/09-FRONTEND.md,docs/protocol/11-OPERATIONS.md | — | DONE d209241 | Dead RPC host rpc.chain.robinhood.com replaced with rpc.mainnet.chain.robinhood.com everywhere; 46646 -> 46630; VITE_ROBINHOOD_* and VITE_CAULDRON_INDEXER documented as dead and removed from .env.example; 11-OPERATIONS 1.1 rewritten from the code. Template gains positionManager, quoteAssets, legacyThresholdEth/legacyBps. verify-manifest now validates quoteAssets decimals. No ABI change. |
| chain-config-fixer | hygiene | public/sitemap.xml | — | NOT STAGED | `npm run build` rewrites this tracked file's <lastmod> to today on every run, so any agent that builds dirties it. Left reverted, not staged. Worth making it a generated artifact. |
| liquidation-gas-fixer | H1/T1a (LiqGasStarved constant floor) | contracts/solidity/cauldron/PerpEngine.sol,contracts/solidity/CauldronHook.sol,contracts/solidity/test/attacks/M1a_LiqGasBand.t.sol | PerpEngine._doSweep/sweepLiquidations, CauldronHook._liqSweep/IPerpEngineLiq | DONE 0f71309 | A PRE-trade in-swap sweep that runs out of gas mid-book now reverts LiqGasStarved instead of degrading: a swap that bankrupts N positions either liquidates all N or reverts. sweepLiquidations returns `bool complete` (selector unchanged; no indexer/src ABI consumer). Sizes: PerpEngine 24,215 -> 24,273 (303 free); CauldronHook 23,258 -> 23,343 (1,233 free). Gas thresholds moved UP: 4-bankrupt-short cascade needs 2.4M (was 1.07M); healthy 4-position book needs 1.3M (was 1.07M). Any gas-sensitive test asserting the old 1.07M boundary will move.
| quote-migration-fixer | T4C (Medium) | contracts/solidity/cauldron/RedemptionExt.sol,contracts/solidity/test/attacks/M4C_QuoteComeHome.t.sol | rotateSliceFrom (fromPrimary derivation, consume, denomination flip) | CLAIMED | Does NOT touch PerpEngine.sol/CauldronHook.sol/GachaLib.sol. No ABI change. |
| chain-config-fixer | RH1A(High),RH1B,RH1C | src/hooks/useAllowedQuotes.ts,src/components/cauldron/TheCauldron.tsx,src/components/cauldron/TreasuryRotation.tsx,src/config/chains.ts,audit/RH_MAINNET_2026-09-16/poc/rh1/rh1_stale_quote_decimals.mjs,audit/RH_MAINNET_2026-09-16/poc/h5/h5_quote_decimals.mjs | useQuoteDecimals, useCurrentQuote, wagmi targetChain transport | DONE bff755a + e598b46 | **API CHANGE (frontend only, no ABI): `useCurrentQuote(gen)` now returns `{ quote, resolved }`, not an `Address`.** Both call sites updated (TheCauldron.tsx:494, TreasuryRotation.tsx:313). `useQuoteDecimals` is keyed to the quote and returns null for a key it has not read — 18 only for the explicit native address, never a seed. The decimals gate in TheCauldron is an AND (resolved && on-chain !== null && no manifest/chain disagreement), so a quote flip makes the trade panel briefly unpriceable by design. targetChain transport is now an ordered `fallback` over CHAIN_PROFILE §8-verified endpoints only. |
| chain-config-fixer | RH1 same-shape sweep (no fix, informational) | src/hooks/useLpComposition.ts,src/hooks/useActivityFeed.ts,src/hooks/useAllowedQuotes.ts | usePoll call sites | RELEASED | `usePoll`'s effect deps are `[intervalMs, enabled]`, so ANY hook whose `load` closes over a changing input keeps serving the old input's answer for ONE poll period. Checked every hook I touched: `useAllowedQuotes` (60s) is safe — `load` deps are `[pc]`, no per-input key. `useLpComposition:173` (8s, `!!generation`) and `useActivityFeed:65` (30s, `!!generation`) have the same shape but windows of 8s/30s, not 300s — NOT claimed, NOT fixed, flagged for whoever owns those files. `useLpComposition` feeds `quoteExpected`, which IS a signed spend amount, so it is the one worth a second look. |

| gacha-expiry-fixer | T3B (High, crystal forfeited after 2 blockhash windows) | contracts/solidity/cauldron/GachaLib.sol,contracts/solidity/test/attacks/M3B_ExpiredCrystalForfeit.t.sol,audit/RH_MAINNET_2026-09-16/poc/h3/M3B_ExpiredCrystalForfeit.t.sol | GachaLib.resolveTickets, new SEED_SLOT/_seed/_pinSeed/_pinSeeds | DONE e0d4c9f | Any single resolveTickets call landing inside a batch's first 256 blocks now PINS blockhash(commitBlock) into a hashed slot, so the outcome is frozen and the 25.9 s wall-clock deadline stops applying (in-swap nativeGachaStep + all 3 router entrypoints already call it). Pinning grants NO extra draw: the pinned value is the original commit seed. The one-re-anchor cap and the terminal forfeit for a batch NO call ever touched are DELIBERATELY unchanged - that is the only ending a peek-and-decline player cannot profit from. GachaLib 1,652 -> 1,916 B. No ABI, event, struct or signature change; CauldronRegistry unchanged at 24,492 (84 free). |
| chain-config-fixer | RH1 same-shape follow-up | src/hooks/useSwapTape.ts,src/hooks/useLpComposition.ts,audit/RH_MAINNET_2026-09-16/poc/rh1/rh1_stale_tape_generation.mjs | useSwapTape state keying, useLpComposition poll gate | DONE 55b0b65 | The trace led past the two named hooks to **useSwapTape**, which IS the signed-price source (tape -> livePerpPrice -> SwapWidget spotPrice -> minOut): its array was unkeyed, so for ~5s after a relaunch the floor came from the DEAD generation's price (~100x too low, nonzero so `minOut <= 0n` misses). Now keyed/invalidated by generation; an unread generation returns [] and TheCauldron falls back to `m.spotPrice`. **BEHAVIOUR CHANGE: useLpComposition no longer gates its poll on `!!generation`** — SwapWidget.tsx:175 and CrystalCauldronGame.tsx:175 pass 0, so it never fetched and every ERC20-quoted buy was permanently unpriceable; those buys are signable again, and two more components now poll `/treasury?fresh=1` every 8s. **useActivityFeed: NO CHANGE NEEDED** — only consumer is `<ActivityDrawer events={activity}>`, display-only. `usePoll` itself is unchanged; see report for the recommended optional `key` param. |

