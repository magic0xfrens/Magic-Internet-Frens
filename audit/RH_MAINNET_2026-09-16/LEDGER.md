# Ledger — RH mainnet review 2026-09-16

## Baseline sizes (forge build --sizes, contracts/solidity @ redteam/2026-09-13 a3bcd29)
| Contract | Runtime (B) | Margin (B) | Claimed | Match |
|---|---|---|---|---|
| CauldronHook | 23,258 | 1,318 | 23,258 (1,318 free) | yes |
| CauldronRegistry | 24,492 | 84 | 24,492 (84 free) | yes |
| PerpEngine | 24,215 | 361 | 24,215 (361 free) | yes |
| PoolOps | 24,173 | 403 | 24,173 (403 free) | yes |
| PerpVault | 11,280 | 13,296 | 11,280 | yes |
| GachaLib | 1,652 | 22,924 | 1,652 | yes |
| PerpSwapLib | 8,968 | 15,608 | 8,968 | yes |

No contract at or over the EIP-170 24,576 limit.

| agent | finding | files (comma-separated) | functions | status | note |
|-------|---------|------------------------|-----------|--------|------|
| chain-config-fixer | T5A,T5B,T5D,T5E,0a,0b,0c | src/config/deployments.ts,src/config/chains.ts,src/config/cauldron.ts,src/config/quotes.ts,src/hooks/useAllowedQuotes.ts,src/components/cauldron/TheCauldron.tsx,src/components/cauldron/SwapWidget.tsx,indexer/ponder.config.ts,indexer/src/api/index.ts,indexer/deployments/round.robinhood.template.json,scripts/verify-manifest.mjs,.env.example,docs/MAINNET_LAUNCH.md,docs/protocol/09-FRONTEND.md,docs/protocol/11-OPERATIONS.md,audit/RH_MAINNET_2026-09-16/poc/h5/* | DEPLOYMENTS/resolveInitialChain, IS_TARGET/ACTIVE_CHAIN, manifest-chain assert, quoteMeta, ponder rpc defaults, API_RPC_URL defaults | DONE | superseded by the 4 rows below; also touched indexer/deployments/active.ts (added to claim). No Solidity, no ABI change. |
| chain-config-fixer | T5A (Critical) | src/config/deployments.ts,src/config/chains.ts,src/config/cauldron.ts,scripts/verify-manifest.mjs | DEPLOYMENTS, resolveInitialChain, CHAIN_DEFAULTS, NATIVE_DECIMALS | DONE 800271a | DEPLOYMENTS is keyed by each manifest's OWN chainId, so a 4663 round.json targets 4663. VITE_CHAIN_ID is authoritative: a chain no manifest declares THROWS at module init and FAILS prebuild. A manifest/chain mismatch now THROWS in cauldron.ts (was console.error) — anything relying on the app still rendering on a mismatch will now blank instead. Per-chain name/RPC/explorer/currency defaults replace the flat Arc ones; unknown chain + no VITE_RPC_URL throws. NATIVE_SYMBOL/NATIVE_DECIMALS read ACTIVE_CHAIN, so VITE_CHAIN_DECIMALS is no longer discarded. No ABI change. |
| chain-config-fixer | T5B (High) | indexer/ponder.config.ts,indexer/src/api/index.ts,indexer/deployments/active.ts | rpc defaults, pollingInterval, API_RPCS, chain guard | DONE f59425e | Sync AND API RPC defaults are keyed by the manifest's chainId (4663 -> rpc.mainnet.chain.robinhood.com, 46630 -> testnet host); an unmapped chainId with PONDER_RPC_URL/API_RPC_URL unset now THROWS at startup instead of silently using Sepolia. active.ts's chain guard covers every DEPLOYMENT slot and honours a CHAIN_ID env declaration. Operators must set PONDER_RPC_URL or add the chain. No ABI change. |
| chain-config-fixer | T5D (Medium) | src/config/quotes.ts,src/hooks/useAllowedQuotes.ts,src/components/cauldron/TheCauldron.tsx,src/components/cauldron/SwapWidget.tsx | quoteMeta, useQuoteDecimals, SwapWidget priceable | DONE 72cff8b | Quote decimals are read on-chain (new useQuoteDecimals -> ERC20 decimals(), validated 0..36, null when unknown) instead of from the manifest. SwapWidget gains quoteDecimalsKnown and is UNPRICEABLE / refuses to sign while decimals are unresolved — a trade panel can now show "cannot price" for a second after a quote flip. quotes.ts reads ACTIVE_ROUND, not round.json. No ABI change. |
| chain-config-fixer | T5E + docs 0a/0b/0c | .env.example,indexer/deployments/round.robinhood.template.json,docs/MAINNET_LAUNCH.md,docs/protocol/09-FRONTEND.md,docs/protocol/11-OPERATIONS.md | — | DONE d209241 | Dead RPC host rpc.chain.robinhood.com replaced with rpc.mainnet.chain.robinhood.com everywhere; 46646 -> 46630; VITE_ROBINHOOD_* and VITE_CAULDRON_INDEXER documented as dead and removed from .env.example; 11-OPERATIONS 1.1 rewritten from the code. Template gains positionManager, quoteAssets, legacyThresholdEth/legacyBps. verify-manifest now validates quoteAssets decimals. No ABI change. |
| chain-config-fixer | hygiene | public/sitemap.xml | — | NOT STAGED | `npm run build` rewrites this tracked file's <lastmod> to today on every run, so any agent that builds dirties it. Left reverted, not staged. Worth making it a generated artifact. |
| liquidation-gas-fixer | H1/T1a (LiqGasStarved constant floor) | contracts/solidity/cauldron/PerpEngine.sol,contracts/solidity/CauldronHook.sol,contracts/solidity/test/attacks/M1a_LiqGasBand.t.sol | PerpEngine._doSweep/sweepLiquidations, CauldronHook._liqSweep/IPerpEngineLiq | DONE 0f71309 | A PRE-trade in-swap sweep that runs out of gas mid-book now reverts LiqGasStarved instead of degrading: a swap that bankrupts N positions either liquidates all N or reverts. sweepLiquidations returns `bool complete` (selector unchanged; no indexer/src ABI consumer). Sizes: PerpEngine 24,215 -> 24,273 (303 free); CauldronHook 23,258 -> 23,343 (1,233 free). Gas thresholds moved UP: 4-bankrupt-short cascade needs 2.4M (was 1.07M); healthy 4-position book needs 1.3M (was 1.07M). Any gas-sensitive test asserting the old 1.07M boundary will move.
| quote-migration-fixer | T4C (Medium) | contracts/solidity/cauldron/RedemptionExt.sol,contracts/solidity/test/attacks/M4C_QuoteComeHome.t.sol | rotateSliceFrom (fromPrimary derivation, consume, denomination flip) | DONE f3d3f9e | "Primary" is now the position holding the CURRENT denomination, not generationPoolKey[gen].currency0 (the launch pair). Before any migration only fromLeg==0 qualifies (bit-identical to old behaviour); after one, only the leg whose quote == generationQuote[gen] does. So a come-home mandate now advances movedPrimaryBps, flips generationQuote back, and deactivates the envelope, which unblocks propose. fromLeg==0 into its own asset still reverts BadConfig (guard untouched). No ABI/event change; sizes unchanged (RedemptionExt 13,323; CauldronRegistry 24,492 / 84 free). RESIDUAL: the generic liveness trap is NOT closed - a mandate that cannot advance for some OTHER reason still blocks propose until expiry; only guardian cancel() clears it. |
| chain-config-fixer | RH1A(High),RH1B,RH1C | src/hooks/useAllowedQuotes.ts,src/components/cauldron/TheCauldron.tsx,src/components/cauldron/TreasuryRotation.tsx,src/config/chains.ts,audit/RH_MAINNET_2026-09-16/poc/rh1/rh1_stale_quote_decimals.mjs,audit/RH_MAINNET_2026-09-16/poc/h5/h5_quote_decimals.mjs | useQuoteDecimals, useCurrentQuote, wagmi targetChain transport | DONE bff755a + e598b46 | **API CHANGE (frontend only, no ABI): `useCurrentQuote(gen)` now returns `{ quote, resolved }`, not an `Address`.** Both call sites updated (TheCauldron.tsx:494, TreasuryRotation.tsx:313). `useQuoteDecimals` is keyed to the quote and returns null for a key it has not read — 18 only for the explicit native address, never a seed. The decimals gate in TheCauldron is an AND (resolved && on-chain !== null && no manifest/chain disagreement), so a quote flip makes the trade panel briefly unpriceable by design. targetChain transport is now an ordered `fallback` over CHAIN_PROFILE §8-verified endpoints only. |
| chain-config-fixer | RH1 same-shape sweep (no fix, informational) | src/hooks/useLpComposition.ts,src/hooks/useActivityFeed.ts,src/hooks/useAllowedQuotes.ts | usePoll call sites | RELEASED | `usePoll`'s effect deps are `[intervalMs, enabled]`, so ANY hook whose `load` closes over a changing input keeps serving the old input's answer for ONE poll period. Checked every hook I touched: `useAllowedQuotes` (60s) is safe — `load` deps are `[pc]`, no per-input key. `useLpComposition:173` (8s, `!!generation`) and `useActivityFeed:65` (30s, `!!generation`) have the same shape but windows of 8s/30s, not 300s — NOT claimed, NOT fixed, flagged for whoever owns those files. `useLpComposition` feeds `quoteExpected`, which IS a signed spend amount, so it is the one worth a second look. |

| gacha-expiry-fixer | T3B (High, crystal forfeited after 2 blockhash windows) | contracts/solidity/cauldron/GachaLib.sol,contracts/solidity/test/attacks/M3B_ExpiredCrystalForfeit.t.sol,audit/RH_MAINNET_2026-09-16/poc/h3/M3B_ExpiredCrystalForfeit.t.sol | GachaLib.resolveTickets, new SEED_SLOT/_seed/_pinSeed/_pinSeeds | DONE e0d4c9f | Any single resolveTickets call landing inside a batch's first 256 blocks now PINS blockhash(commitBlock) into a hashed slot, so the outcome is frozen and the 25.9 s wall-clock deadline stops applying (in-swap nativeGachaStep + all 3 router entrypoints already call it). Pinning grants NO extra draw: the pinned value is the original commit seed. The one-re-anchor cap and the terminal forfeit for a batch NO call ever touched are DELIBERATELY unchanged - that is the only ending a peek-and-decline player cannot profit from. GachaLib 1,652 -> 1,916 B. No ABI, event, struct or signature change; CauldronRegistry unchanged at 24,492 (84 free). |
| chain-config-fixer | RH1 same-shape follow-up | src/hooks/useSwapTape.ts,src/hooks/useLpComposition.ts,audit/RH_MAINNET_2026-09-16/poc/rh1/rh1_stale_tape_generation.mjs | useSwapTape state keying, useLpComposition poll gate | DONE 55b0b65 | The trace led past the two named hooks to **useSwapTape**, which IS the signed-price source (tape -> livePerpPrice -> SwapWidget spotPrice -> minOut): its array was unkeyed, so for ~5s after a relaunch the floor came from the DEAD generation's price (~100x too low, nonzero so `minOut <= 0n` misses). Now keyed/invalidated by generation; an unread generation returns [] and TheCauldron falls back to `m.spotPrice`. **BEHAVIOUR CHANGE: useLpComposition no longer gates its poll on `!!generation`** — SwapWidget.tsx:175 and CrystalCauldronGame.tsx:175 pass 0, so it never fetched and every ERC20-quoted buy was permanently unpriceable; those buys are signable again, and two more components now poll `/treasury?fresh=1` every 8s. **useActivityFeed: NO CHANGE NEEDED** — only consumer is `<ActivityDrawer events={activity}>`, display-only. `usePoll` itself is unchanged; see report for the recommended optional `key` param. |

| liquidation-gas-fixer | E1A (Critical, push-spot forced liquidation drains PLV) + E2A check | contracts/solidity/cauldron/PerpEngine.sol,contracts/solidity/test/attacks/E1B_LiqDrainScale.t.sol | PerpEngine._liqTest (spot leg clamp), PerpEngine._settle (band on MODE_LIQUIDATION) | CLAIMED | PerpEngine.sol only, still mine. No ABI/event/struct change. |
| quote-migration-fixer | T4C-b (Medium, liveness trap) | contracts/solidity/cauldron/TreasuryGovernor.sol,contracts/solidity/test/attacks/M4E_StalledMandate.t.sol,contracts/solidity/test/attacks/M4A_RelaunchSeam.t.sol,contracts/solidity/test/attacks/M4B_RelaunchWhale.t.sol | stalled() [new view], propose, execute | DONE 696899b | New public view stalled() = envelope is active, movedBps==0, movedPrimaryBps==0, and now >= lastEnvelopeAt+COOLDOWN. Both governance gates (propose :437, execute :617) now read "... && !stalled()", so a mandate that has moved NOTHING stops blocking at its cooldown instead of at its 30-day expiry -- no guardian needed. ONE successful slice of any size out of any leg by any address sets movedBps!=0 and immunises the envelope permanently, so a griefer cannot supersede an advancing mandate. Nothing is cancelled: the envelope stays active and spendable; guardian cancel() is unchanged as the backstop. ADDITIVE ABI only (new view stalled()); no existing signature or event changed, so indexer/ and src/ need no update. Sizes: TreasuryGovernor 6,646 -> 6,878 (+232 B, 17,698 free); CauldronRegistry 24,492 / 84 free UNCHANGED; PoolOps 24,173 / 403 UNCHANGED; RedemptionExt 13,323. Tests: test/attacks/M4E_StalledMandate.t.sol 3/3 green, and with the two gates neutralised test_unexecutable_mandate_does_not_wedge_propose FAILS ProposalActive() while both negative controls still pass. All 15 existing test/TreasuryGovernor.t.sol tests green incl. test_OneEnvelopeAtATime. M4A/M4B/M4C green; M4A+M4B now committed. RESIDUAL: a mandate whose PRIMARY is stuck but whose secondary legs still rotate has movedBps!=0 and is not cleared by this path -- guardian cancel() remains for that case. |
| e1a-fixer-2 | E1A (Critical, push-spot forced liquidation drains PLV) | contracts/solidity/cauldron/PerpEngine.sol,contracts/solidity/test/attacks/E1B_LiqDrainScale.t.sol | PerpEngine._liqTest (spot leg gated off the in-swap sweep) | CLAIMED | taking over liquidation-gas-fixer's uncommitted E1A work on the same files (its row above stays CLAIMED but is superseded here). No ABI/event/struct change. |
| chain-config-fixer | OL-1 (Critical), OL-3, OL-4 | indexer/scripts/patch-ponder-finality.mjs,indexer/scripts/patch-ponder-finality.d.mts,indexer/package.json,indexer/start.mjs,indexer/src/api/index.ts,indexer/seed-keeper.mjs,docs/protocol/11-OPERATIONS.md,audit/RH_MAINNET_2026-09-16/poc/ol/ol_liveness.mjs | finality patch, freshness lag fields, poke chain identity | DONE 286056b | **Ponder's reorg tolerance is NOT configurable (VERIFIED, 0.11.44)** — patched in `node_modules` by a committed, idempotent, self-verifying script run from `postinstall` AND `start.mjs`; **failure is fatal, the indexer refuses to boot**. 4663/46630 -> 12,000 blocks, Arc -> 1,200. **If you upgrade Ponder, re-run `node indexer/scripts/patch-ponder-finality.mjs` — it fails loudly if upstream changed shape.** `/freshness` gained chainHeight/indexedHeight/lagBlocks/lagSeconds/finalizedHeight/finalityLagBlocks/finalityBlockCount/reorgToleranceOk and now **503s when the live finality lag exceeds the tolerance** — anything matching on that JSON should read the new fields. `startSeedKeeper()` is **async now** and proves its chain id before signing; a mismatch refuses loudly instead of "poke skipped" forever. Level-triggered senders left level-triggered (asserted in the test). |
| chain-config-fixer | OL-2 (High) | scripts/keeper.sh | watch-loop cadence | DONE 097e58d | Gacha resolve cadence is derived from the chain's block time against the 256-block commit window (4663: 6s vs a 25.6s window; Sepolia unchanged at 64s; unknown chain assumes the slow case). Anyone tuning this loop: the constraint is "at least 4x inside 256 blocks", not a fixed number of seconds. |
| chain-config-fixer | Railway restart cap / round.json chainId | indexer/railway.json,indexer/deployments/round.json | — | NO CHANGE | `restartPolicyMaxRetries: 10` left as-is: the in-process watchdog deliberately relies on that cap to bound a restart loop, so raising it trades "stays down" for "restarts forever" — an owner decision, documented in 11-OPERATIONS 1.1b. `round.json` pinning chainId 11155111 is CORRECT — it is the live Sepolia manifest and every consumer is keyed by the manifest's own chainId; the cutover replaces the file. |
| chain-config-fixer | B-1 (High), B-6 copy, B-2 claim | src/config/chains.ts,src/components/cauldron/CrystalCauldronGame.tsx,src/components/cauldron/SwapWidget.tsx,src/config/presale.ts,audit/RH_MAINNET_2026-09-16/poc/cb/cb_coherence.mjs | COMMIT_WINDOW_*, settle copy, unpriceable copy, maxPerWallet | DONE 20e8bc3 | **NEW EXPORTS in src/config/chains.ts: `COMMIT_WINDOW_BLOCKS` (256), `COMMIT_WINDOW_SECONDS` (number\|null), `COMMIT_WINDOW_PHRASE`** — use these for any settle/expiry copy; never hardcode a duration, 256 blocks is 26s on 4663 and 51min on Sepolia. `CHAIN_DEFAULTS` gained `blockMs`. **`PRESALE.maxPerWallet` is now `number \| null` and is `null`** — any UI claiming a per-wallet limit must read `MAX_PER_WALLET()` from the chain. B-6 needed no gate change (closed by 55b0b65); only its copy was wrong. |
| chain-config-fixer | B-5, B-8 | indexer/src/api/index.ts | /legacy buffer scaling, /floor ticker | DONE 1c53fed | **API SHAPE CHANGE (indexer JSON, no ABI): /legacy gains `bufferRaw`, `bufferDecimals`, `bufferQuote`, `bufferUnitMatchesThreshold`; `bufferEth` is now scaled by the live quote's own decimals; `bufferPct` is 0 when the buffer and the manifest ether threshold are different units** (it used to divide them anyway and render 0%). Frontend consumers: `useCollectionFloor.ts`, `CollectionFloorPanel.tsx`, `TheCauldron.tsx:1012`. /floor's `ticker` falls back to the live token's `symbol()` instead of the generation-1-pinned presale ticker. |
| chain-config-fixer | B-2 (High) | contracts/solidity/cauldron/MiFrensGenesis.sol | MAX_PER_WALLET | NOT FIXABLE IN CODE | **Owner decision required before the mainnet presale.** VERIFIED by `cast call` on the live contract 0xfd488978…92ba: `MAX_PER_WALLET() = 1111 = GENESIS_SUPPLY()`, `minted() = 1111` (Sepolia genesis is SOLD OUT, so the deployed instance is moot). `MAX_PER_WALLET` is `immutable` (MiFrensGenesis.sol:119, set at :243) with NO setter — it is a CONSTRUCTOR ARGUMENT. To have a real anti-whale cap on 4663 the mainnet deploy must pass a value well below `genesisSupply`; otherwise one wallet can take the whole genesis supply and 1111 votes. I did not touch Solidity. |
| quote-migration-fixer | B-3 (High) + E2A | contracts/solidity/cauldron/MiFrensGenesis.sol,contracts/solidity/test/attacks/V2A_VoteFarm.t.sol | _update, _getVotingUnits, new _votableBalance | CLAIMED | Voting units suppressed at the source for non-genesis ids. Does NOT touch PerpEngine.sol/CauldronHook.sol/GachaLib.sol/TreasuryGovernor.sol. |
| chain-config-fixer | CA-1 (High) | indexer/src/index.ts,indexer/src/api/index.ts | Opened/Closed handlers, liveStats, lpEthOf | DONE f5580de | Perp collateral/payout/pnl are scaled by the GENERATION'S quote decimals (new `quoteDecimalsForGen`, on-chain, validated 0..36, cached per gen but NEVER after a failed read). `liveStats` scales OI/PLV/depth by the live quote's decimals; `plvToken` and `size` deliberately keep 18 (brew token, not quote). **API SHAPE: the perp payload now carries `quoteSymbol`, `quoteDecimals`, `quote`.** `*Eth` field names KEPT on purpose — renaming touches every consumer for zero behavioural gain, so the unit travels in the data instead. |
| chain-config-fixer | CA-2 + liquidation disclosure | src/hooks/usePerpEngine.ts,src/components/cauldron/PerpPanel.tsx,audit/RH_MAINNET_2026-09-16/poc/ca/ca_perp_decimals.mjs | qGlyph, PositionCard, pp-note | DONE 2508a6b | `PerpStats` gains `quoteSymbol`/`quoteDecimals`; PerpPanel derives `qGlyph` and passes it to `PositionCard` (new optional prop). 19 hardcoded Ξ replaced; the 10 left are comments or inside a `qNative ?` branch. New disclosure paragraph next to the open button: a stranger's swap can liquidate you in their own tx at a PROJECTED price. |
| chain-config-fixer | 1e18/formatEther sweep (informational) | indexer/src/api/index.ts,src/components/cauldron/ActivityDrawer.tsx,src/components/cauldron/SpellFeed.tsx,src/hooks/useActivityFeed.ts | — | RELEASED | Remaining hits on QUOTE-denominated values, NOT fixed, for whoever owns them: `ActivityDrawer.tsx:104` and `SpellFeed.tsx:116` `formatEther(e.quoteWei)`, fed by `useActivityFeed.ts:49` `BigInt(Math.round(r.amountEth * 1e18))` — a float re-inflated to "wei" on both sides, so the feed's amounts are 1e12 off on a 6-dec quote (display only, no signature). Confirmed CORRECT and not to be "fixed": `api/index.ts:1588,1620` (vault `q()`/`n()` split is right), `plvToken`, `size`, `reserveTokens`, `StakePanel` token maths, `usePerpVault.depositEth` (native value), `vaultEth` (PerpVault holds native ETH via `depositEth`). |
| e1a-final-fixer | E1A (Critical, push-spot forced liquidation drains PLV) | contracts/solidity/cauldron/PerpEngine.sol,contracts/solidity/test/attacks/E1B_LiqDrainScale.t.sol | PerpEngine._settle (SPLIT band: own backing unbanded, insurance+plv tranche banded), PerpEngine.liquidate/_liqTest (comments only) | CLAIMED | final fixer, takes over e1a-fixer-2 + liquidation-gas-fixer rows on the same two files (both superseded here). No ABI/event/struct change. |
