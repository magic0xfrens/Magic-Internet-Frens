# CRITICALS — FINAL_BLIND_2026-09-13
(append a line here the moment a Critical is CONFIRMED)

- ~~**T3c**~~ **DOWNGRADED to HIGH by V3** (not permanent: the quote-side staker's own `PerpVault.withdrawEth` + a permissionless `syncGeneration` heals it, and a native-pinned engine revives at the next relaunch because `generationQuote[newGen]` defaults to `address(0)`; ceiling is one generation; NOT recoverable by owner/timelock/registry) — original claim: (hunter H3, VERIFIED) — 1 wei of `PerpVault` quote-side stake permanently strands the perp engine on the first quote rotation: `RedemptionExt.sol:582` flips `generationQuote`, `:617` calls `syncGeneration` best-effort, `PerpEngine.sol:1337` reverts `VaultStaked()` on any `hasQuoteStake()`, the catch swallows it, `_isDead()` (`:1797`) reads true forever, `_guardOpen` (`:1748`) reverts `TokenDead`, and `setVault(0)` (`:2435`) is vetoed by `hasStakers()`. `deploy/DeployPerp.s.sol:235` creates the blocking staker itself. Every open position becomes `forceCloseDead`-able by a stranger at `minOut = 0`. PoC `test/attacks/K3c_RotationStrandsPerpEngine.t.sol`.

- ~~**K5b**~~ **DOWNGRADED to MEDIUM by V5** (the dead seeder is a documented design choice: `PoolOps.sol:157-167` "Lower this below 1e18 to bring the streamed launch back — the machinery is intact — but anything that hosts perps wants it at 1e18"; anti-snipe is delegated to the surtax + LaunchSniper. What SURVIVES: the `fundPrime` trap — callable by the seeder's deployer EOA or the registry owner (`CauldronSeeder.sol:338-339`), `deploy/DeployLaunchpad.s.sol:536-541` tells the operator to "Send 2-3Ξ", `PRIME_BUY_ETH` defaults to 0 so the script does not fund it by default, and both native egresses (`:632` behind `seeding()`, `:842` reverting) are closed — an irreversible loss of protocol funds contradicting the NatSpec at `:335-336`. SEPARATELY: 26 baseline test failures are this design choice never propagated to the suite, hidden by `vm.skip` on the fork gate — see BASELINE_FAILURES.md) — original claim: (hunter H5, VERIFIED) — the progressive launch seeder is unreachable: `PoolOps.sol:168 SEED_BASE_WAD = 1e18` makes `baseTok == activeTokens` and `baseEth == ethAmount`, so the guard at `PoolOps.sol:405` always returns and `:410` — the only call site of `CauldronSeeder.startSeed` — never runs. Every launch is atomic; the streamed anti-snipe ladder, `poke`/`pokeInSwap`, and the treasury prime buy are inert. `fundPrime` still accepts ETH with no exit: `withdrawAll` is gated on `seeding()` (false forever) and `rescueSeeder()` reverts. 1.0 ETH trapped in the PoC. 5 of 6 `test/ProgressiveSeed.t.sol` tests fail under the fork env but open with `vm.skip`. PoC `test/attacks/K5b_ProgressiveSeederUnreachable.t.sol`.
